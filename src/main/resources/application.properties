spring.application.name=SIDOC

# Perfil por defecto (dev)
spring.profiles.active=dev

# =========================
# DB (DEV) - MySQL local
# =========================
# Si todav�a est�s con Oracle en local, comenta esto y usa tu config anterior solo en application-dev.properties.
spring.datasource.url=jdbc:mysql://caboose.proxy.rlwy.net:34657/railway?useSSL=true&requireSSL=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=jonffZQvSKeAYOIipRDyiizRfsBYYSQc
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# =========================
# FTP (DEV)
# =========================
sidoc.ftp.host=${DEV_FTP_HOST:127.0.0.1}
sidoc.ftp.port=${DEV_FTP_PORT:21}
sidoc.ftp.user=${DEV_FTP_USER:user}
sidoc.ftp.password=${DEV_FTP_PASSWORD:password}
sidoc.ftp.passive=${DEV_FTP_PASSIVE:true}

# =========================
# RUTAS FTP
# =========================
sidoc.ruta.archivos=/FTP_BIBLIOTECA/Reportes-Doctrina/manuales/archivos/
sidoc.ruta.portadas=/FTP_BIBLIOTECA/Reportes-Doctrina/manuales/portadas/

# =========================
# MULTIPART
# =========================
spring.servlet.multipart.max-file-size=512MB
spring.servlet.multipart.max-request-size=512MB

# =========================
# APIs (NO poner keys aqu�)
# =========================
openai.api.key=${OPENAI_API_KEY:}
openai.api.key2=${OPENAI_API_KEY2:}


# Logging
spring.security.oauth2.resourceserver.jwt.issuer-uri=${ISSUER_URI:}
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=${JWKS_URI:}
app.cors.allowed-origins=${ALLOWED_ORIGINS:}